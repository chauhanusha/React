export default function Header(){
    return(
        <header className="header">
            <nav>
            <img src="src/react-logo.jpg" alt="react logo"/>
            <span>ReactFacts</span>
            </nav>

        </header>
    )
}